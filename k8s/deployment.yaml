apiVersion: apps/v1
kind: Deployment
metadata:
  name: minha-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: minha-app
  template:
    metadata:
      labels:
        app: minha-app
    spec:
      containers:
        - name: minha-app
          image: ${{ inputs.STACK_TYPE }}-app:${{ github.sha }}
          imagePullPolicy: Always
          ports:
            - containerPort: 3000